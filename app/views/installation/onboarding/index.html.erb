<!DOCTYPE html>
<html>
  <head>
    <title>Super Admin | I9PRO</title>
    <%= javascript_pack_tag 'superadmin' %>
    <%= stylesheet_pack_tag 'superadmin' %>
  </head>
  <body data-gr-c-s-loaded="true">
    <div id="app" class="superadmin-body app-wrapper app-root">
      <div class="medium column login">
        <div class="text-center medium-12 login__hero align-self-top">
          <img
            src="/brand-assets/logo.svg"
            alt="Chatwoot logo"
            class="hero__logo"
          />
          <h2 class="hero--title">
            Olá, Bem-vindo ao I9PRO 👋
          </h2>
        </div>
        <div class="row align-center">
          <div class="small-12 medium-4 column">
            <%= form_tag('/installation/onboarding', class: 'login-box column align-self-top') do %>
              <div class="column log-in-form">
                <% if flash[:error].present? %>
                <div data-alert class="alert-box warning"><%= flash[:error] %></div>
                <% end %>
                <label>
                  <span>Nome</span>
                  <%= text_field :user, :name, placeholder: "Informe o seu nome e sobrenome. ex: Thiago Almeida", required: true %>
                </label>
                <label>
                  <span>Nome da empresa</span>
                  <%= text_field :user, :company, placeholder: "Informe o nome da sua empresa. ex: Inovmi", required: true %>
                </label>
                <label>
                  <span>E-mail de trabalho</span>
                  <%= email_field :user, :email, placeholder: "Informe o seu e-mail de acesso. eg: suporte@inovmi.com.br", required: true %>
                </label>
                <label>
                  <span>Password</span>
                  <%= password_field :user, :password, placeholder: "Informe uma senha com no mínimo 6 caracteres.", required: true %>
                </label>
                <div class="update-subscription--checkbox">
                  <%= check_box_tag "subscribe_to_updates", 'true', true %>
                  <label for="subscribe_to_updates">
                    Assine notas de lançamento, boletins informativos e pesquisas de feedback de produtos.
                  </label>
                </div>
                <button type="submit" class="button nice large expanded">
                  Finalizar instalação
                </button>
              </div>
            <% end %>
          </div>
        </div>
    </div>
  </body>
</html>
